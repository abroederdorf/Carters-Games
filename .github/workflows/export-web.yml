name: Export & Deploy

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.6.2
    steps:
      - uses: actions/checkout@v4

      - name: Setup export templates
        run: |
          mkdir -v -p ~/.local/share/godot/export_templates/
          cp -r /root/.local/share/godot/export_templates/. ~/.local/share/godot/export_templates/

      - name: Export
        run: |
          mkdir -v -p web
          godot --headless --export-release "Web" web/index.html

      - name: Upload export artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-export
          path: web/

  deploy-netlify:
    needs: export
    runs-on: ubuntu-latest
    steps:
      - name: Download export artifact
        uses: actions/download-artifact@v4
        with:
          name: web-export
          path: web/

      - name: Deploy to Netlify
        run: |
          npm install -g netlify-cli
          netlify deploy --dir=web --prod --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

  deploy-itchio:
    needs: export
    runs-on: ubuntu-latest
    steps:
      - name: Download export artifact
        uses: actions/download-artifact@v4
        with:
          name: web-export
          path: web/

      - name: Install butler
        run: |
          curl -L -o butler.zip https://github.com/itchio/butler/releases/latest/download/butler-linux-amd64.zip
          unzip butler.zip
          chmod +x linux-amd64/butler

      - name: Deploy to itch.io
        run: ./linux-amd64/butler push web/ abroederdorf/carters-games:html5
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_CREDENTIALS }}
